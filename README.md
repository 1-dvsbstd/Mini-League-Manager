# Mini League Manager

A shareable league-member app for FPL side competitions. Each league opens through its tracker key.

This is an early prelaunch build. It uses the shared Baruc test backend while the product is being validated.
